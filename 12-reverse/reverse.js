/* The reverse() method is used to reverse the order of the elements in an array. It mutates the original array. */

const fruits = ["fig", "apple", "banana", "orange"];
fruits.reverse()
console.log(fruits)

//reverse() reverses the order of the elements in the fruits array. The fruits array is now [‘orange’, ‘banana’, ‘apple’, ‘fig’].